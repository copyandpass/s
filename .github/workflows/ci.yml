name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Run unit tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.11, 3.13]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install pytest pytest-asyncio

      - name: Run tests
        env:
          GEMINI_API_KEY: ${{ 'AIzaSyDyjdlEpCmVzkfC1OZzKEmspZR_ZL0_Cco' }}
          DB_HOST: 127.0.0.1
          DB_USER: root
          DB_PASSWORD: 1234
          DB_NAME: soncoding_db
          # 이 부분이 핵심입니다: 현재 디렉토리(.)를 파이썬 경로에 추가
          PYTHONPATH: .
        run: |
          pytest -q --junitxml=pytest-report.xml

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          # 이름 뒤에 파이썬 버전을 붙여서 중복을 피합니다.
          name: pytest-report-python-${{ matrix.python-version }}
          path: pytest-report.xml
